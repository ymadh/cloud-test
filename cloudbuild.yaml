steps:
  # Install
  - name: "gcr.io/cloud-builders/npm"
    args: ["install"]
  # Test
  - name: "gcr.io/cloud-builders/npm"
    args: ["test"]
    env:
      - "CI=true"
  # Build
  - name: "gcr.io/cloud-builders/npm"
    args: ["app", "deploy", "--ignore-file=cloudbuild.yaml"]
# Deploy
